<Window x:Class="BG_BIM_Interface.UI.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BG_BIM_Interface.UI"
             mc:Ignorable="d" 
             Height="300" Width="200">
    <Grid>
        <StackPanel Margin = "10" VerticalAlignment="Center">
            <Label VerticalAlignment="Top" HorizontalAlignment="Center" Content= "{Binding Counter}"/>
            <Button VerticalAlignment="Top" HorizontalAlignment="Center" Content= "ClickMe" Command="{Binding CountUpCommand}"/>
            <TextBox Text="{Binding MyText}" Margin = "10"/>
        </StackPanel>        
    </Grid>
</Window>
